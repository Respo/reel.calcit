# Upgrade respo-ui to 0.7.12

- Align Calcit and `@calcit/procs` at 0.13.63 and consume the cycle-free respo-ui 0.7.12 release.
- Replace the setup-calcit commit hash with stable `@v1`, pin Yarn 4.12.0, and enforce strict dependency resolution in CI.
- Migrate the remaining local `tag-match` calls to exhaustive `match`.
