# Reel 0.6.11 release / Reel 0.6.11 发版

- Release the strict Option-boundary changes merged in PR #32.
- 发布 PR #32 合入的严格 Option 边界改动。
- Align the package metadata in `deps.cirru` and `package.json` at version `0.6.11`.
- 将 `deps.cirru` 与 `package.json` 的包版本统一为 `0.6.11`。
